* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background: #f4f1ec;
  color: #111;
  font-family: Arial, "Microsoft YaHei", sans-serif;
}

.hero {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  padding: 60px;
  background: linear-gradient(135deg, #111 0%, #2a230d 45%, #c7a23a 100%);
  color: white;
  overflow: hidden;
}

.tag {
  font-size: 14px;
  letter-spacing: 2px;
  margin-bottom: 24px;
  color: #f5d46b;
}

.hero h1 {
  font-size: 88px;
  line-height: 0.95;
  font-weight: 900;
  margin-bottom: 28px;
}

.subtitle {
  font-size: 20px;
  line-height: 1.7;
  max-width: 520px;
}

.hero-media video {
  width: 100%;
  border-radius: 28px;
  box-shadow: 0 30px 80px rgba(0,0,0,0.35);
}

.section {
  padding: 100px 8%;
}

.section h2 {
  font-size: 42px;
  margin-bottom: 28px;
  letter-spacing: 1px;
}

.section p {
  font-size: 18px;
  line-height: 1.9;
  max-width: 900px;
  color: #444;
  margin-bottom: 36px;
}

.wide-img {
  width: 100%;
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.08);
  background: white;
}

.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}

.card {
  background: white;
  border-radius: 24px;
  padding: 20px;
  box-shadow: 0 16px 40px rgba(0,0,0,0.07);
}

.card img {
  width: 100%;
  border-radius: 18px;
  margin-bottom: 18px;
}

.card h3 {
  font-size: 20px;
  margin-bottom: 8px;
}

.card p {
  font-size: 15px;
  margin: 0;
  color: #666;
}

.summary {
  background: #111;
  color: white;
}

.summary p {
  color: #ddd;
}

@media (max-width: 900px) {
  .hero {
    grid-template-columns: 1fr;
    padding: 36px;
  }

  .hero h1 {
    font-size: 56px;
  }

  .grid {
    grid-template-columns: 1fr;
  }

  .section {
    padding: 70px 6%;
  }
}